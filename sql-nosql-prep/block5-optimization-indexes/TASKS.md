# 📝 Задачи: Оптимизация и индексы

## Уровень 1: Создание индексов (задачи 1-3)

### Задача 5.1
Создайте индекс для ускорения поиска клиентов по email.

```sql
-- Напишите запрос здесь
```

---

### Задача 5.2
Создайте составной индекс для поиска заказов по клиенту и дате.

```sql
-- Напишите запрос здесь
```

---

### Задача 5.3
Создайте уникальный индекс для email клиентов.

```sql
-- Напишите запрос здесь
```

---

## Уровень 2: EXPLAIN (задачи 4-6)

### Задача 5.4
Используйте EXPLAIN для анализа запроса поиска по email.

```sql
-- Напишите запрос здесь
```

---

### Задача 5.5
Сравните план выполнения с индексом и без.

```sql
-- Напишите запрос здесь
```

---

### Задача 5.6
Определите тип сканирования (Seq Scan или Index Scan).

```sql
-- Напишите запрос здесь
```

---

## Уровень 3: Оптимизация запросов (задачи 7-10)

### Задача 5.7
Оптимизируйте запрос: найдите заказы за последнюю неделю.

```sql
-- ПЛОХО
SELECT * FROM orders WHERE DATE(order_date) >= CURRENT_DATE - 7;

-- Оптимизируйте здесь:
```

---

### Задача 5.8
Оптимизируйте запрос с LIKE.

```sql
-- ПЛОХО
SELECT * FROM customers WHERE name LIKE '%Иван%';

-- Подсказка: используйте полнотекстовый поиск или измените требование
```

---

### Задача 5.9
Исправьте N+1 проблему.

```python
# ПЛОХО
customers = db.query("SELECT * FROM customers")
for c in customers:
    orders = db.query(f"SELECT * FROM orders WHERE customer_id = {c.id}")

# Напишите один запрос с JOIN:
```

---

### Задача 5.10
Оптимизируйте подсчёт количества completed заказов.

```sql
-- Медленно
SELECT COUNT(*) FROM orders WHERE status = 'completed';

-- Предложите оптимизацию с индексом:
```

---

## ✅ Чек-лист

- [ ] Создание индексов
- [ ] EXPLAIN ANALYZE
- [ ] Типы сканирования
- [ ] Оптимизация WHERE
- [ ] N+1 проблема
- [ ] Частичные индексы
